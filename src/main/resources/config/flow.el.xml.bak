<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <chain name="chain1">
        THEN(
            a,
            WHEN(
                    b,
                    SWITCH(c).to(d,e)
                ),
            f
        );
    </chain>
</flow>